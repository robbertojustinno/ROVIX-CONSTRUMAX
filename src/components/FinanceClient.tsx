"use client";import { useEffect,useState } from "react";export default function FinanceClient(){const[data,setData]=useState<any>({receivables:[],payables:[]});useEffect(()=>{fetch("/api/finance").then(r=>r.json()).then(setData)},[]);return <><h1>Financeiro</h1><div className="grid"><div className="card"><h3>Contas a receber</h3>{data.receivables.map((r:any)=><p key={r.id}>#{r.id} {r.party} — R$ {r.amount} — {r.due_date} — {r.status}</p>)}</div><div className="card"><h3>Contas a pagar</h3>{data.payables.map((r:any)=><p key={r.id}>#{r.id} {r.party} — R$ {r.amount} — {r.due_date} — {r.status}</p>)}</div></div></>}
