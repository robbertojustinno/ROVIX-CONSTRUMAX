"use client";import { useState } from "react";import { useRouter } from "next/navigation";
export default function Login(){const r=useRouter();const[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})});if(res.ok)r.push("/dashboard");else setError((await res.json()).error)}return <div className="login"><div className="card"><h1>ROVIX CONSTRUMAX</h1><p className="muted">Acesso ao sistema</p><form onSubmit={submit}><p><input className="input" name="email" type="email" placeholder="E-mail" required/></p><p><input className="input" name="password" type="password" placeholder="Senha" required/></p>{error&&<p>{error}</p>}<button className="btn">Entrar</button></form></div></div>}
